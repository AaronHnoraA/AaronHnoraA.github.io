# Agent DB

`org-roam.sqlite3` is a repository-local link to the current org-roam database:

```text
../../.emacs.d/var/org/org-roam.db
```

Use it as a read-only index for `nodes` and `links`. The Org files remain the source of truth.
